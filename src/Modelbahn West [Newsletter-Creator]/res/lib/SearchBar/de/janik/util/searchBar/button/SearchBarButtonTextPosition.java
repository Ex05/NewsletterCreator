package de.janik.util.searchBar.button;

// <- Import ->
// <- Static_Import ->

/**
 * @author Jan.Marcel.Janik [©2014]
 *
 */
public enum SearchBarButtonTextPosition
{
	// <- Public ->
	LEFT(), RIGHT(), CENTER(), TOP(), BOTTOM(), OVER();

	// <- Protected ->
	// <- Private->
	// <- Static ->
	// <- Constructor ->
	// <- Abstract ->
	// <- Object ->
	// <- Getter & Setter ->
	// <- Static ->
}
